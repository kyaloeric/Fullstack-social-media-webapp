-- Create Database Tweet;
-- Use Tweet;

-- CREATE TABLE Likes (
  
--     like_id VARCHAR(200) PRIMARY KEY,
--     tweet_id VARCHAR(200),
--     user_id VARCHAR(200),
--     date_time DATETIME,
--     CONSTRAINT FK_Like_Tweet FOREIGN KEY (tweet_id) REFERENCES Tweet(tweet_id),
--     CONSTRAINT FK_Like_User FOREIGN KEY (user_id) REFERENCES Users(user_id)
--   );
