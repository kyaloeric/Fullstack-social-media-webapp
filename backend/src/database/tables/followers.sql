-- USE Tweet

--   CREATE TABLE Follower (
--     follower_id VARCHAR(200) PRIMARY KEY,
--     follower_user_id VARCHAR(200),
--     following_user_id VARCHAR(200),
--     CONSTRAINT FK_Follower_User FOREIGN KEY (follower_user_id) REFERENCES Users(user_id),
--     CONSTRAINT FK_Following_User FOREIGN KEY (following_user_id) REFERENCES Users(user_id)
--   );
